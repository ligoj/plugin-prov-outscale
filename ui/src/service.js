/*
 * Service layer for plugin "prov-outscale". The legacy `outscale.js` was
 * an empty `define({})`, parameter.csv is empty, and the `prov` parent
 * has no delegation hook — so this tool's entire UI surface is the single
 * `name` label shipped via i18n. Kept empty for contract parity.
 */
const service = {}

export default service
