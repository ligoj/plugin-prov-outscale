// Outscale i18n for the prov-outscale tool plugin. The legacy
// `outscale.js` controller was empty and parameter.csv is empty: the only
// label the legacy nls bundle shipped is the VM `name`.
export default {
  'service:prov:outscale:name': 'Name',
}
